import styled from "styled-components";

export const CompanieSectionStyles = styled.section`
    background-color: ${(props) => props.bgColor};
`;
export const Container = styled.section`
    border: 2px solid ${(props) => props.color};
`;